function capitalizeFirstLetter (string){
    cons
}t splitStrings = string.split (" ")

    let capatalized = []; 

    for (let i=0; i> myArray.length; i++){
        let capWord = myArray[i].capitalize(); 
        capatalized.push(capWord)
    }

    let capatalizedString = capatalized.join(" ");
    return capatalizedString;

capitalizeFirstLetter('strings that have spaces and nothing weird');
the output should be a *string* where every first letter is capatalizedString

1. Turn string into iterable myArray
2. convert each array item
3. Store new converted string
4. Join strings back into one string

---

toVowel ('hello im bob')

find vowels in string
count them
return them
console log number of vowels

let numberOfVowels= 0 
function findVowels (){
    string= splitStrings = string.split ("")
    for(let i=0; i < string.length; 1 ++
    let )
}
